# Простая инструкция: как работает Яблочный пресет

Тут всё про то, как пользоваться этим расширением и не бояться, что что-то сломается.

## 1. Как сохраняются настройки?
Когда ты в Таверне нажимаешь на галочки (тоглы) в настройках пресета:
- Они **сразу сохраняются** в твоем браузере (или в файле настроек Таверны).
- Тебе **не нужно** нажимать «Сохранить» где-то еще, если ты просто меняешь настройки внутри расширения.

## 2. Что такое «Синхронизация» (Sync)?
В расширении есть кнопка «Синхронизировать пресет». Когда ты её жмешь:
1. Расширение берет «базовый файл» (тот, что я обновляю на GitHub).
2. Накладывает сверху **твои настройки** (какой язык выбран, какие тоглы включены).
3. Создает или обновляет сам пресет (который `🍎Yablochny Preset`) в списке пресетов Таверны.

## 3. Мои настройки удалятся при обновлении?
**НЕТ.**
- Если я обновлю код на Гитхабе и ты его себе скачаешь (через `git pull` или мои команды), твои включенные галочки **останутся**.
- Если ты сама добавила какие-то свои промпты в самом пресете Таверны — расширение при синхронизации постарается их **не удалять**, а оставить рядом с моими.

## 4. Как правильно обновлять на GitHub?
Если ты сама что-то поменяла в коде или в файле `🍎 Yablochny Preset.json` и хочешь отправить это в интернет:
- Используй команду `/push` в чате со мной. 
- Она просто берет всё, что лежит у тебя в папке, и копирует это на GitHub.
- **Главное:** Перед пушем убедись, что твои изменения работают (просто проверь в Таверне).

## 5. Если всё сломалось (Кнопка паники)
Если пресет перестал работать или всё перепуталось:
1. Удали сам пресет `🍎Yablochny Preset` из списка пресетов Таверны (через мусорку).
2. Нажми кнопку «Синхронизировать» в настройках расширения.
3. Он создастся заново чистенький и по моим последним правилам.

---
**Короче:** Тыкай кнопки, меняй настройки — ничего не пропадет. Если хочешь «сохранить для вечности» на Гитхабе — пиши `/push`.
