# Руководство пользователя: Yablochny Preset & Extension

Данный документ содержит техническое описание установки, настройки и эксплуатации пресета и соответствующего расширения для SillyTavern.

---

## 1. Установка и обновление системы

### 1.1 Первичная установка
Установка производится исключительно через встроенный менеджер расширений SillyTavern:
1. Перейдите в меню **"Кубики"** (Extensions) -> **Install Extension**.
2. Укажите URL данного репозитория и нажмите **Install**.
3. После завершения установки необходимо **перезагрузить страницу** браузера.
4. Откройте панель настроек расширения (в меню "Кубики") и нажмите кнопку **"Sync preset"**. 
5. В интерфейсе выбора пресетов Chat Completion (**Вкладка А**) выберите появившийся профиль **"🍎Yablochny Preset"**.

### 1.2 Процесс обновления
Обновление осуществляется следующим образом:
1. Перейдите в меню **"Кубики"** -> **Manage Extensions**.
2. Найдите в списке **Yablochny Preset** и нажмите кнопку **Update**.
3. **Обязательно перезагрузите страницу** после завершения загрузки файлов.
4. Проверьте актуальность настроек в панели расширения. Нажатие кнопки **"Sync preset"** рекомендуется, но не всегда обязательно (расширение автоматически проверяет версию пресета при запуске).

---

## 2. Настройка расширения

### 2.1 Кнопка "Sync preset"
Функционал кнопки заключается в генерации финального пресета на основе базового шаблона и выбранных пользователем параметров.
*   **Важно:** Синхронизация не удаляет кастомные промпты, добавленные пользователем во Вкладку А вручную. Они будут сохранены в итоговом файле.
*   Если пресет работает некорректно, рекомендуется удалить его через Вкладку А и нажать **Sync preset** для чистой переустановки.

### 2.2 Встроенный Regex Manager
Расширение включает в себя функционал управления регулярными выражениями (Regex):
*   Активация производится кнопкой **"Регексы ВКЛ"**.
*   В блоке **Regex packs** необходимо отметить интересующие пакеты форматирования.
*   Скрипты автоматически интегрируются в основной раздел Regex скриптов SillyTavern под префиксом `[RM]`.

---

## 3. Настройка параметров генерации (Вкладка А)

### 3.1 Система индикации (Символы)
Внутренняя структура пресета использует следующие обозначения для упрощения навигации:
*   **◈ (System)**: Критические компоненты логики. Отключение запрещено.
*   **↗ / ↘ / → (Option)**: Группа взаимоисключающих параметров. Должен быть активен только один пункт из группы.
*   **┌ / └ (Connection)**: Визуальная привязка зависимых промптов.
*   **◦ (Personal)**: Опциональные параметры стиля и оформления.

### 3.2 Рекомендации по моделям
Для стабильной работы на различных API необходимо скорректировать настройки во Вкладке А:

*   **GPT-4 / GPT-o1**:
    *   Рекомендуется отключение тогла **COT (Chain of Thought)** в конце списка.
    *   Активируйте **GPT JB** в начале пресета.
    *   В блоке патчей активируйте **GPT anti-echo**.
    *   В поле **Prefill** (внизу страницы) установите значение `no COT GPT prefill`.

*   **Gemini**:
    *   Активируйте патч **Gemini double quotes** для корректной обработки диалогов.
    *   В поле **Prefill** установите `Gemini prefill`.

*   **Claude (Anthropic)**:
    *   Используйте стандартный `universal prefill`.
    *   Наличие `<think>` в начале ответа (prefill) допустимо только для моделей с поддержкой рассуждений.

---

## 4. Общие рекомендации
*   Любые изменения в настройках расширения требуют времени на сохранение. Рекомендуется дождаться уведомления об успешной синхронизации.
*   При переключении языков (RU/EN/UA) через расширение, изменения вступят в силу после нажатия кнопки синхронизации.
