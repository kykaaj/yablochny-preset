# 🍎 Adaptive Preset — SillyTavern Extension

Расширение, которое управляет пресетом Chat Completion. 
Обновления промптов приходят через GitHub, настройки юзера не сбрасываются.

## Установка

1. Открой SillyTavern
2. Иди в **Extensions** → **Install Extension**  
3. Вставь ссылку на GitHub репозиторий:
   ```
   https://github.com/YourName/SillyTavern-AdaptivePreset
   ```
4. Нажми **Install**
5. В списке пресетов Chat Completion появится **🍎 Adaptive**

## Как это работает

- Файл `defaults.json` содержит все промпты с номерами версий
- Когда ты (автор) обновляешь промпт — увеличиваешь `version` в `defaults.json`
- Юзер нажимает **Update** в управлении расширениями
- Расширение автоматически обновляет только изменённые промпты
- Какие тоглы юзер включил/выключил — **не сбрасывается**

## Для автора: как обновить промпт

1. Открой `defaults.json`
2. Найди нужный промпт по `identifier`
3. Измени `content` (текст промпта)
4. **Увеличь `version`** на 1 (было 3 → стало 4)
5. Сохрани файл и запуш на GitHub

Пример:
```json
{
    "identifier": "nsfw",
    "name": "◈ NSFW Prompt",
    "role": "system",
    "content": "Тут новый текст промпта...",
    "version": 4,          ← было 3, стало 4
    "default_enabled": true
}
```

## Для автора: как добавить новый промпт

1. Добавь новый объект в массив `prompts` в `defaults.json`
2. Задай уникальный `identifier` (можно UUID)
3. Добавь его в `prompt_order` в нужное место
4. Запуш на GitHub

## Файлы расширения

| Файл | Что делает |
|---|---|
| `manifest.json` | Метаданные расширения |
| `defaults.json` | Мастер-файл с промптами и настройками (ЭТО ты редактируешь) |
| `index.js` | Логика синхронизации |
| `settings.html` | HTML панели настроек |
| `style.css` | Стили |
